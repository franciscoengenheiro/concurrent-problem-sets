/*
 * This variable only serves to see the lazy implementation in the Kotlin library.
 * It was considered necessary to place it here in order to understand how this
 * implementation uses synchronization mechanisms with and without locks.
 */
private val seeLazyImplementation: Boolean by lazy { true }